# Dotfiles

Để cài đặt và tự động thiết lập toàn bộ hệ thống, bạn chỉ cần chạy duy nhất lệnh sau:

```bash
./bootstrap.sh --user <tên_user_của_bạn>
```
*(Nếu không truyền tham số `--user`, script sẽ tự động lấy username hiện tại của tài khoản).*
